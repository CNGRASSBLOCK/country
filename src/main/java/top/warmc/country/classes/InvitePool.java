package top.warmc.country.classes;

import java.util.HashMap;
import java.util.Map;
import java.util.UUID;

public class InvitePool {
    private static final Map<UUID, Town> Invite_TownToPlayer = new HashMap<>();
    private static final Map<UUID, Town> Invite_PlayerToTown = new HashMap<>();


}
